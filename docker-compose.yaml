version: '3.8'

services:
  angular-ionic-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8100:80"
    volumes:
      - ./:/app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

